# typst-template-report-lab
Typst template for lab reports in laboratories.

To initialize a new report locally use `init.sh`, which will copy the template
of the report to the specified path.
```bash
./init.sh <path>
```

